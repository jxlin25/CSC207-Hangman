package app;

public class HangmanGame {
    private String wordToguess;
    private set<Character> guessed;
    private int remaining attempts;
    private int boolean gameOver;

}

public HangmanGame(String wordToguess);
            this.wordToguess = wordToguess;
            this set<Character> guessed = <newHashset>;
            this.remainingAttempts = 6;
        this.gameOver = false;


public boolean guessLetter(char letter);
letter = Character.toUpperCase(letter);
            if guessLetters.contains(letter));
        system.out.printin("Letter alredady guessed");
                return False