package use_case.endgameresults;

// A marker class, as no specific input is needed to fetch the results from the DAO.
public class EndGameResultsInputData {
    // Empty, but mandatory placeholder.
}
